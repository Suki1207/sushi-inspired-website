{
  "name": "sushi_themed_website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && git add dist && git commit -m 'Deploy Vite app to master' && git push origin master"
  },
  "devDependencies": {
    "vite": "^5.0.8"
  },
  "dependencies": {
    "aos": "^3.0.0-beta.6"
  }
}
